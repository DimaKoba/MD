# Plugin loadImage

#### Code examples
```
const image = new loadImages({
            wrapper: '.images-block',
            delay: 3000,
            speed: 0.7,
            width: '200px',
            height: '200px'
        });

        image.init();

```
